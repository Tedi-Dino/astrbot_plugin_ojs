{
  "00_tip": {
    "description": "🔴 强烈建议：多群组复杂配置请直接修改 data/config/astrbot_plugin_ojs_config.json 文件，比在网页点点点更方便！详见：github.com/Tedi-Dino/astrbot_plugin_ojs/blob/master/README.md",
    "type": "string",
    "default": "请阅读上方红色提示",
    "obvious_hint": true
  },
  "group_configs": {
    "description": "多群独立配置列表 (点击加号添加)",
    "type": "list",
    "hint": "建议直接去后台改 JSON 文件",
    "items": {
      "type": "object",
      "description": "单个群配置",
      "items": {
        "group_id": {
          "description": "群号",
          "type": "string",
          "hint": "生效的群号"
        },
        "target_users": {
          "description": "目标用户列表",
          "type": "list",
          "items": {
            "type": "string"
          },
          "hint": "QQ号列表"
        },
        "reply_probability": {
          "description": "触发概率 (0.0 - 1.0)",
          "type": "float",
          "default": 0.2
        },
        "limit_once_per_day": {
          "description": "每日仅触发一次 (04:00刷新)",
          "type": "bool",
          "default": false,
          "hint": "开启后，该规则每天最多生效一次"
        },
        "reply_list": {
          "description": "回复内容列表",
          "type": "list",
          "items": {
            "type": "string"
          },
          "default": [
            "收到",
            "确实"
          ]
        }
      }
    }
  },
  "max_per_minute": {
    "description": "单群每分钟最大回复数",
    "type": "int",
    "default": 10,
    "hint": "全局频率限制，防止刷屏"
  }
}
